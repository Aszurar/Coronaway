import React from 'react';
import { Container, TextInput } from './styles';

const Input: React.FC = () => (
    <Container>
        <TextInput>Olá</TextInput>
    </Container>
);

export default Input;
